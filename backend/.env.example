# Optional: add these for universal audio fingerprinting.
# Without them, the prototype still tries page metadata on YouTube Music,
# YouTube, Spotify Web, and SoundCloud.

PORT=8787
HOST=127.0.0.1

ACR_HOST=identify-eu-west-1.acrcloud.com
ACR_ACCESS_KEY=
ACR_ACCESS_SECRET=
